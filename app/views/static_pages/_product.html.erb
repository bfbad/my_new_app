<div class="item <%= 'active' if active %>">
  <%= image_tag(product.image_url) %>
  <div class="carousel-caption">
    <%= product.name %> <%= link_to "Edit", edit_product_path(product.id) %> | <%= link_to "Show", product %>
  </div>
</div>